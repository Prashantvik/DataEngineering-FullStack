This folder houses the configuration files that centralize and organize various settings for your data engineering project. The configurations include database connection details, API keys, environment variables, and other sensitive information required by the project's components.

Folder Structure (Make changes accordingly)
The structure of the config folder is as follows:

config/
├── api_keys.json
├── database.yaml
├── environment_variables.env
